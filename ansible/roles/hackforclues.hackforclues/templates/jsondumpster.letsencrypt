domains = {{ hackforclues_hostname }}
email = {{ hackforclues_email }}
# Disable ncurses
text = True
# authenticate by placing a file in the webroot (under
# .well-known/acme-challenge/) and then letting letsencrypt fetch it
authenticator = webroot
webroot-path = {{ hackforclues_letsencrypt_webroot_path }}